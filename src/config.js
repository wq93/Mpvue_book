// 配置项

//const host = 'http://localhost:5757'
const host = 'https://4jrajotd.qcloud.la'

const loginUrl = `${host}/weapp/login`
const userUrl = `${host}/weapp/user`
const config = {
  host, loginUrl,userUrl
}
export default config
